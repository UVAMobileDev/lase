import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

export default function WaferLog(props) {
    return (
        <View>
            <Text>Future home of the WaferLog.</Text>
        </View>
    );
}
