import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

export default function Utilities(props) {
    return (
        <View>
            <Text>Future home of the utilities component</Text>
        </View>
    );
}

const styles = StyleSheet.create({
    container: {

    }
});
